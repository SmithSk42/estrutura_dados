Bubblesort 

public insertionsort
for (int = i; i<array.legnt; i++){
    for(int = j; j < array.legnt-1-i; j++){
        if(array[i] < array[i+1]){
            swap(i, i+1);
        }
    }
}

public void swap(int a, int b){
    int temp = array[a];
    array[a] = array [b];
    array[b] = temp;
}